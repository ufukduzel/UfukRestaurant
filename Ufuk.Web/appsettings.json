{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  //45th step.
  "ServiceUrls": {
    //"ProductAPI": "https://localhost:7088/" // We can remove the forward slash at the very end.
    //The best place to debug is where the call is actually made. (BaseService Class) 
    "ProductAPI": "https://localhost:7088" // 97th step.
  }
}
